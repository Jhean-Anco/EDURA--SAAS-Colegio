# URL del backend NestJS (sin trailing slash)
BACKEND_URL=http://localhost:3000

# Secreto para cifrar cookies iron-session (mínimo 32 caracteres)
# Genera uno con: openssl rand -hex 32
SESSION_SECRET=cambia_esto_por_un_secreto_de_32_caracteres_minimo

# Entorno
NODE_ENV=development
